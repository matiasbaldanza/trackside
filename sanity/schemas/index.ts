import type { SchemaTypeDefinition } from "sanity";

/**
 * The schema registry.
 *
 * Documents and objects are added in Milestone 2. The Studio runs with an
 * empty registry, which is the honest state of the project right now: the
 * editorial surface exists and has nothing to edit yet.
 */
export const schemaTypes: SchemaTypeDefinition[] = [];
